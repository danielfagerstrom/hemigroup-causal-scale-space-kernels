% Appendix A — Memory kernels, Sonine conservation, and the scale-Cauchy problem
% (Theorem 3'). Draft §§9–10 (lines 320–511) moved TOGETHER (NOTES-exposition Part 4:
% a 21-node leaf block — no main-line theorem depends on it; §10 is built on §9, so
% splitting them creates a cross-appendix reference for nothing). Lemma 9.1 is lifted
% OUT (to §7's tail); X₀, T_r, 𝒟 are lifted out (to §3). Everything else stays, in
% full, with proofs — this is the article's least-formalized region, so its proofs must
% be printed (appendix yes, blueprint deferral no).
% Contents: scale evolution in memory-kernel form (Prop 9.2), potential kernel, Sonine
% conservation (Thm 9.5) and exact invertibility, pair regularity, the Volterra
% derivation (Prop 9.8), literature (Rmk 9.11); then the delay core (Lemma 10.1), the
% Phillips form, the scale-Cauchy problem (Thm 10.4 = Theorem 3'), fixed-scale
% semigroups, the Gamma time-recursive form, and the recovery of 2005's Theorem 3.

\section{Memory kernels, Sonine conservation, and the scale-Cauchy problem}\label{sec:appendix-memory}

% [transcription pending]
