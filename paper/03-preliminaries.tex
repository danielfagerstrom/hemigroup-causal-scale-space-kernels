% §3 Preliminaries — draft §2 (lines 57–94), plus the causal-space vocabulary lifted from
% draft §10 head (lines 436–443): X₀ = L¹(ℝ₊), the delay semigroup T_r, and the core 𝒟 —
% NOTES-exposition Part 4 repair 1, so that §9 (signaling) cites nothing in the appendix.
%
% Keep: Definitions 2.1–2.2 (CM, BF), Proposition 2.3 (toolbox) with the "which vocabulary
% does the work" paragraph (already written in the draft), Lemma 2.4 (vanishing).
% Shared statements: def:completely-monotone? — check blueprint part 02 for the exact
% labels; every labelled statement here carries `% shared with blueprint <label>`.

\section{Preliminaries and notation}\label{sec:preliminaries}

% [transcription pending]
