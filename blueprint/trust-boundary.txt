# The trust boundary: the analytic interfaces a headline theorem may depend on.
#
# `#print axioms` on this article's proved theorems must reduce to Lean core plus exactly
# these. CI reads this file via `linkage axioms`, which refuses any name that no AXIOMS.md
# entry mentions in a `**Lean:**` segment — so the boundary cannot be widened without a
# reviewed ledger entry behind it.
#
# Adding a line here widens the project's trust base and is a review decision, not a fix.
#
# A17 (2026-08-09), reviewed in AXIOMS.md against SSV Theorem 5.2, p. 49. The existence half of
# the subordinator correspondence: a Levy triple is realised by a finite measure with the
# matching Laplace transform. It is what thm:main-characterization's constructive direction
# needs to build the kernels, and the only thing it needs that is not proved here.
#
# Note what is NOT on this line. The source's theorem gives sub-probability measures; the
# upgrade to a probability measure is prose in SSV and is proved here instead. Uniqueness of the
# measure is likewise proved, not trusted. And the axiom is stated in this development's own
# vocabulary -- levyExponent, laplace, IsCausal, no CompletelyMonotone -- so that the day the
# compound-Poisson construction is carried out it can be demoted to a lemma and this file
# returned to empty, without touching a single downstream statement.

Hemigroup.exists_isFiniteMeasure_laplace_eq_exp_neg_levyExponent

# A18 (2026-08-10), a REVIEW DECISION taken deliberately and recorded in AXIOMS.md. Self-
# decomposability, in the direction the analysis needs: a Levy exponent whose dilation increments
# F(b.) - F(a.) are all Levy exponents has a Levy measure with a nonincreasing density k(t)/t.
# It is the blueprint's lem:selfdecomposable-derivative (1) => (3), and the only thing
# thm:main-analysis needs that is not proved here.
#
# Note what is NOT on this line. The converse, (3) => (1), is PROVED -- levyExponentD_increment
# -- so thm:main-construction and prop:main-uniqueness carry A17 and not this. The article's
# claim that the analysis direction crosses the boundary where the constructive one does not is
# therefore checked by CIAxiomGuard rather than asserted in prose.
#
# Unlike A17 this entry is expected to be PERMANENT. A17 was phrased so it could be demoted once
# the compound-Poisson construction is done, which Mathlib's Prokhorov puts within reach; A18's
# (1) => (2) leg needs differentiability of Bernstein functions, which is the derivative-sign
# vocabulary this development excludes by design. The AXIOMS.md entry says so plainly.
#
# Anchored on SSV Proposition 5.17, p. 57, with Definition 5.14, p. 55 -- image-verified
# 2026-08-10. Printed as an iff; we use the hard half. The source allows a killing term and
# we do not, so our hypothesis is a special case of its. The k(t)/t normalisation is the
# source's own, from the proof on p. 59. Sato Theorem 15.10, p. 95, is the R^d ancestor and
# is NOT the anchor: it carries no numbered half-line specialisation.

Hemigroup.exists_antitone_density_of_dilation_increments
