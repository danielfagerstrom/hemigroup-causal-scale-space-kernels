# Axiom provenance ledger — hemigroup causal scale-space kernels

The project's **trust boundary**. Every `[A]` analytic-interface node in the blueprint is
grounded here, in a named theorem with a page anchor, resolved through the librarian.

Format contract (enforced by `linkage check`, fatal checks 2 and 6):

```
## A<N> — <one-line statement of what is taken on trust>
**Blueprint:** `<label>` · **Lean:** `<decl>`
**Cite:** @<citekey> — <page anchor>

- **Statement as used.** …
- **Primary — <the named theorem>.** …
```

A `[A]` node must also declare, in its status annotation, what the citation carries and what
it does not (`\textbf{Assignment.}`, fatal check 7). Widening this file widens the trust
base; it is a review decision, not a fix.

---

*No entries yet.* The analytic spine this article will need is largely Bernstein-function
theory — complete monotonicity, the Lévy–Khintchine representation for Bernstein functions,
self-decomposability — for which Schilling–Song–Vondraček is the natural primary and Feller
Vol. 2 §XIII the corroboration. Both are already in the library; resolve citekeys through the
librarian before writing an entry.

Note the overlap with `scale-space-foundations`: its A5 pins the same Bernstein theorem
(Feller Vol. 2 §XIII.4 Thm 1, pp. 439–440). Entries here are still this article's own — the
ledger is per-article by design, since each article's `[A]` set is exactly what *its* results
rest on — but the page anchors should agree, and a disagreement is a signal that one of them
is wrong.
