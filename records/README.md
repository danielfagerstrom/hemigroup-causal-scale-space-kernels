# Records

What the work produced as a record rather than as instructions: external reviews archived
verbatim, response plans, process accounts behind the AI statement, audits, fix lists, reports.
One directory per module (`records/<module>/`). A record is written once and not revised; no
instruction file quotes it, and a session reads it only when a handoff names it. The release export
ships the module's records.
